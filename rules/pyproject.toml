[project]
name = "lockpick-xlator"
version = "0.1.0"
description = "Translate government policy documents into machine-readable rulesets"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "rapidfuzz>=3.0",
    "rich>=10.0",
]
