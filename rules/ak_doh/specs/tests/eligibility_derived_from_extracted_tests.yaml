# Auto-generated by /expand-tests. Do not edit directly.
# Re-run /expand-tests to regenerate.
#
# NOTE: Tests in this file are inferred from narrative context in extracted-tests.yaml
# (header comments, notes: fields, partial examples). They are best-effort and may
# need manual review. Do not treat this file as a complete specification.
#
# Source: ext_001 (domains/ak_doh/specs/extracted-tests.yaml).
# ext_001 itself is already present in eligibility_tests.yaml.
# The cases below are additional scenarios derivable from the ext_001 notes:
#   - The 10% compatibility band is [1536.75, 1878.25] around client adjusted $1,707.50.
#   - Any DOL adjusted income outside this band with client below the limit → manual_verification.
#   - A1E 2025 income_limit = $1,795; client exactly at limit → approve with DOL at or below.
test_suite:
  spec: "eligibility.civil.yaml"
  description: "Eligibility derived tests inferred from 523B policy example in extracted-tests.yaml — 2026-03-27"
  version: "1.0"

# =============================================================================
# Reference (A1E 2025): income_limit = $1,795
# Client chain: (gross - 20 - 65) × 0.5 → adjusted
# Compatibility band: abs(dol_adj - client_adj) <= client_adj * 0.10
# ext_001 band: client=1707.50, 10%=170.75, range [1536.75, 1878.25]
# =============================================================================

tests:

  # ---------------------------------------------------------------------------
  # drv_001 — DOL income just outside the 10% compatibility band (above upper bound)
  # From ext_001 notes: band upper = 1707.50 × 1.10 = 1878.25.
  # DOL adjusted just above this = $1,879 → outside band → manual_verification.
  # DOL gross to produce adjusted=1879: gross = 1879 × 2 + 85 = 3843 → (3843-20-65)×0.5=1879 ✓
  # Client: same as ext_001 (adjusted=1707.50 < 1795 A1E 2025 limit) → below standard.
  # Result: client ≤ limit, DOL > limit, NOT compatible → manual_verification.
  # ---------------------------------------------------------------------------
  - case_id: "drv_001"
    description: "A1E 2025: client adjusted $1,707.50 below standard; DOL adjusted $1,879 just outside 10% band (upper=1878.25) — manual verification required"
    source:
      file: "domains/ak_doh/specs/extracted-tests.yaml"
      section: "ext_001 notes — 10% compatibility band analysis"
    inputs:
      ClientData.gross_earned_income: 3500
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11529
      DOLRecord.gross_earned_income: 3843
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "manual_verification"
      reasons: []
    tags: ["derived", "manual_verification", "action_4", "incompatible", "client_below_dol_above"]
    notes: "DOL adjusted = (3843-20-65)×0.5 = 1879; above upper compatibility bound of 1878.25. dol_quarterly_earnings = 3843×3 = 11529."

  # ---------------------------------------------------------------------------
  # drv_002 — Client income exactly at A1E 2025 income limit ($1,795)
  # From ext_001: year is 2025, A1E income_limit = $1,795.
  # Gross to produce adjusted=1795: gross = 1795×2+85 = 3675 → (3675-20-65)×0.5=1795 ✓
  # DOL same as client → both at limit (≤) → approve (523B action 1).
  # ---------------------------------------------------------------------------
  - case_id: "drv_002"
    description: "A1E 2025: client adjusted income exactly $1,795 = income limit; DOL same — approve (≤ limit passes)"
    source:
      file: "domains/ak_doh/specs/extracted-tests.yaml"
      section: "ext_001 — A1E 2025 income_limit reference"
    inputs:
      ClientData.gross_earned_income: 3675
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11025
      DOLRecord.gross_earned_income: 3675
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["derived", "allow", "boundary", "at_income_limit", "a1e_2025"]
