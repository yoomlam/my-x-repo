# Auto-generated by /expand-tests. Do not edit directly.
# Re-run /expand-tests to regenerate.
#
# NOTE: Tests in this file are inferred from narrative context in extracted-tests.yaml
# (header comments, notes: fields, partial examples). They are best-effort and may
# need manual review. Do not treat this file as a complete specification.
#
# Source: domains/ak_doh/specs/extracted-tests.yaml — ext_001 notes field.
# The 523B policy example traces the full exclusion chain for both client and DOL income.
# ext_001 inputs are for the eligibility module; the values below exercise the
# exclusion_chain sub-module directly using the same gross figures.
test_suite:
  spec: "exclusion_chain.civil.yaml"
  description: "Exclusion chain derived tests inferred from 523B policy example in extracted-tests.yaml — 2026-03-27"
  version: "1.0"

tests:

  # ---------------------------------------------------------------------------
  # drv_001 — Client income from 523B example (ext_001)
  # Source: ext_001 notes: "client $3,500 → -$20 → -$65 → ×0.5 = $1,707.50"
  # gross=3500, non_needs_based_unearned_remainder=20
  # (3500 - 20 - 65) × 0.5 = 3415 × 0.5 = 1707.50
  # ---------------------------------------------------------------------------
  - case_id: "drv_001"
    description: "523B example — client gross $3,500 with $20 general exclusion: (3500 - 20 - 65) × 0.5 = $1,707.50"
    source:
      file: "domains/ak_doh/specs/extracted-tests.yaml"
      section: "ext_001 notes — client income trace"
    inputs:
      gross_earned_income: 3500
      non_needs_based_unearned_remainder: 20
    expected:
      adjusted_earned_income: 1707.50
    tags: ["derived", "allow", "steps_5_6_8"]

  # ---------------------------------------------------------------------------
  # drv_002 — DOL average monthly income from 523B example (ext_001)
  # Source: ext_001 notes: "DOL monthly avg = $11,200/3 = $3,733.33 → -$20 → -$65 → ×0.5 = $1,824.17"
  # gross=3733.33, non_needs_based_unearned_remainder=20
  # (3733.33 - 20 - 65) × 0.5 = 3648.33 × 0.5 = 1824.165
  # Note: document states $1,824.17 due to rounding in presentation.
  # ---------------------------------------------------------------------------
  - case_id: "drv_002"
    description: "523B example — DOL avg monthly gross $3,733.33 (=$11,200/3) with $20 general exclusion: (3733.33 - 20 - 65) × 0.5 = $1,824.165"
    source:
      file: "domains/ak_doh/specs/extracted-tests.yaml"
      section: "ext_001 notes — DOL income trace"
    inputs:
      gross_earned_income: 3733.33
      non_needs_based_unearned_remainder: 20
    expected:
      adjusted_earned_income: 1824.17
    tags: ["derived", "allow", "steps_5_6_8"]
    notes: "3648.33 × 0.5 = 1824.165; Catala money rounds half-up → $1,824.17. Policy document also states $1,824.17."
