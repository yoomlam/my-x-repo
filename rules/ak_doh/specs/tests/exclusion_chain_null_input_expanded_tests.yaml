# Auto-generated by /expand-tests. Do not edit directly.
# Re-run /expand-tests to regenerate.
#
# CATALA BACKEND NOTE: This module uses the Catala transpiler, not OPA/Rego.
# Null-input behavior differs from OPA:
#   - Missing required fields cause Catala uninitialized-variable errors, NOT runtime denial.
#   - Wrong-type inputs (string for money) are Catala parse errors caught at
#     test-generation time, not runtime.
#   - nil_001 is flagged as a Catala error case and will not produce a valid output
#     from the test runner; it is included for documentation purposes.
# The most meaningful tests here are negative-value inputs (nil_002), which are
# structurally valid and exercise the min() clamping behavior.
test_suite:
  spec: "exclusion_chain.civil.yaml"
  description: "Null and malformed input tests — generated 2026-03-27"
  version: "1.0"

tests:

  # ---------------------------------------------------------------------------
  # nil_001 — Required field omitted: gross_earned_income missing
  # Catala behavior: uninitialized-variable error ("no rule applies" for gross_earned_income).
  # NOTE: This test is NOT expected to run cleanly through the Catala test runner.
  # It documents the required-field constraint; the Catala clerk will raise an exception.
  # ---------------------------------------------------------------------------
  - case_id: "nil_001"
    description: "Required field gross_earned_income omitted — Catala uninitialized-variable error expected"
    inputs:
      non_needs_based_unearned_remainder: 20
    expected:
      adjusted_earned_income: null
    tags: ["null_input", "catala_error", "missing_required_field"]
    notes: "Catala raises an uninitialized-variable exception, not a runtime denial. Test runner will report an error, not a pass/fail on adjusted_earned_income."

  # ---------------------------------------------------------------------------
  # nil_002 — Negative gross_earned_income ($-1)
  # Structurally valid Catala input; exercises min() clamping at step 6.
  # Computation with gross=-1, no optional fields:
  #   after_general_20 = -1 - min(0, 20) = -1
  #   after_65 = -1 - min(-1, 65) = -1 - (-1) = 0   ← min clamps at -1
  #   after_irwe = 0, after_half = 0, after_blind = 0
  #   adjusted_earned_income = 0
  # ---------------------------------------------------------------------------
  - case_id: "nil_002"
    description: "Negative gross income ($-1): step 6 min() clamp drives result to $0. adjusted_earned_income = 0"
    inputs:
      gross_earned_income: -1
    expected:
      adjusted_earned_income: 0
    tags: ["malformed", "negative_income", "allow"]
    notes: "Negative gross is semantically invalid per policy but structurally valid in Catala. The min() expression at step 6 absorbs the negative, producing adjusted=0."
