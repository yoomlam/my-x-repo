# Auto-generated by /expand-tests. Do not edit directly.
# Re-run /expand-tests to regenerate.
test_suite:
  spec: "eligibility.civil.yaml"
  description: "Edge case expanded tests — generated 2026-03-27"
  version: "1.0"

# =============================================================================
# Key reference (simple chain, steps 5+6+8):
#   adjusted = (gross - 20 - 65) × 0.5
# =============================================================================

tests:

  # ---------------------------------------------------------------------------
  # edg_001 — Zero client and DOL income
  # ClientData.gross_earned_income=0 → exclusion_chain: 0 - 20 = -20 → after_65=0 → adjusted=0
  # 0 ≤ 1845 (A1E 2026) → ALLOW fires → approve
  # ---------------------------------------------------------------------------
  - case_id: "edg_001"
    description: "Zero gross income (client and DOL): exclusion chain clamps to $0; $0 ≤ $1,845 limit — approve"
    inputs:
      ClientData.gross_earned_income: 0
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 0
      DOLRecord.gross_earned_income: 0
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["edge", "allow", "zero_income"]

  # ---------------------------------------------------------------------------
  # edg_002 — Extreme income: 10× the highest couple income limit
  # Highest income_limit = $2,732 (A2S 2026). 10× = $27,320.
  # gross = 27320 × 2 + 85 = 54725
  # (54725 - 20 - 65) × 0.5 = 54640 × 0.5 = 27320 >> 2732 → deny (both above)
  # ---------------------------------------------------------------------------
  - case_id: "edg_002"
    description: "Extreme income — 10× highest limit: client and DOL adjusted $27,320 (A2S 2026 limit=$2,732) — deny (both above standard)"
    inputs:
      ClientData.gross_earned_income: 54725
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 164175
      DOLRecord.gross_earned_income: 54725
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["edge", "deny", "extreme_income", "couple_type"]

  # ---------------------------------------------------------------------------
  # edg_003 — H1E household type (H-type, single; income_limit = $1,845 in 2026)
  # No existing tests use H1E household type.
  # (2000 - 20 - 65) × 0.5 = 957.50 < 1845 → approve
  # ---------------------------------------------------------------------------
  - case_id: "edg_003"
    description: "H1E household type (H-type single, 2026): adjusted $957.50 below $1,845 limit — approve. Covers H-type table lookup."
    inputs:
      ClientData.gross_earned_income: 2000
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "H1E"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 6000
      DOLRecord.gross_earned_income: 2000
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["edge", "allow", "h1e", "household_type_coverage"]

  # ---------------------------------------------------------------------------
  # edg_004 — NHR household type (2026, income_limit = $1,845)
  # No existing tests use NHR household type.
  # (2000 - 20 - 65) × 0.5 = 957.50 < 1845 → approve
  # ---------------------------------------------------------------------------
  - case_id: "edg_004"
    description: "NHR household type (2026): adjusted $957.50 below $1,845 limit — approve. Covers NHR table lookup."
    inputs:
      ClientData.gross_earned_income: 2000
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "NHR"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 6000
      DOLRecord.gross_earned_income: 2000
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["edge", "allow", "nhr", "household_type_coverage"]

  # ---------------------------------------------------------------------------
  # edg_005 — All optional exclusion fields set simultaneously (A1E 2026)
  # Verifies all 10 exclusion steps fire together and keep income below limit.
  # Step-by-step (client):
  #   gross=5000
  #   Step 1: 5000 - 100 (federal)  = 4900
  #   Step 2: 4900 - 50  (eitc)     = 4850
  #   Step 3: 4850 - 10  (irreg)    = 4840
  #   Step 4: 4840 - 200 (student)  = 4640
  #   Step 5: 4640 - 20             = 4620
  #   Step 6: 4620 - 65             = 4555
  #   Step 7: 4555 - 300 (irwe)     = 4255
  #   Step 8: 4255 × 0.5            = 2127.50
  #   Step 9: 2127.50 - 200 (blind) = 1927.50
  #   Step 10: 1927.50 - 100 (ss)   = 1827.50 ≤ 1845 → approve
  # DOL same inputs.
  # ---------------------------------------------------------------------------
  - case_id: "edg_005"
    description: "All 10 exclusion steps applied: federal ($100), EITC ($50), irregular ($10), student ($200), $20 general, $65 earned, IRWE ($300), one-half, blind work ($200), self-support ($100). Client and DOL adjusted $1,827.50 ≤ $1,845 — approve"
    inputs:
      ClientData.gross_earned_income: 5000
      ClientData.federal_exclusions: 100
      ClientData.eitc_exclusion: 50
      ClientData.irregular_earned_income: 10
      ClientData.student_earned_income: 200
      ClientData.student_monthly_limit: 2410
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.impairment_work_expenses: 300
      ClientData.blind_work_expenses: 200
      ClientData.self_support_exclusion: 100
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 15000
      DOLRecord.gross_earned_income: 5000
      DOLRecord.federal_exclusions: 100
      DOLRecord.eitc_exclusion: 50
      DOLRecord.irregular_earned_income: 10
      DOLRecord.student_earned_income: 200
      DOLRecord.student_monthly_limit: 2410
      DOLRecord.non_needs_based_unearned_remainder: 20
      DOLRecord.impairment_work_expenses: 300
      DOLRecord.blind_work_expenses: 200
      DOLRecord.self_support_exclusion: 100
    expected:
      eligible: "approve"
      reasons: []
    tags: ["edge", "allow", "all_exclusions", "combined_steps"]
