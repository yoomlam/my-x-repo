# Auto-generated by /expand-tests. Do not edit directly.
# Re-run /expand-tests to regenerate.
test_suite:
  spec: "exclusion_chain.civil.yaml"
  description: "Edge case expanded tests — generated 2026-03-27"
  version: "1.0"

tests:

  # ---------------------------------------------------------------------------
  # edg_001 — Extreme gross income (10× the highest income standard for eligibility)
  # Highest income_limit in eligibility table = $2,732 (A2S 2026).
  # 10× = $27,320 → gross = 27320 × 2 + 65 = 54,705 (no optional exclusions)
  # (54705 - 65) × 0.5 = 54640 × 0.5 = 27320.00
  # ---------------------------------------------------------------------------
  - case_id: "edg_001"
    description: "Extreme gross income ($54,705) — 10× highest eligibility standard; no optional exclusions. Result: (54705 - 65) × 0.5 = $27,320.00"
    inputs:
      gross_earned_income: 54705
    expected:
      adjusted_earned_income: 27320.00
    tags: ["edge", "allow", "extreme_income"]

  # ---------------------------------------------------------------------------
  # edg_002 — All 10 exclusion steps at non-trivial values (distinct from full_chain_001)
  # gross=10000, all exclusions set:
  #   Step 1: 10000 - 500 (federal) = 9500
  #   Step 2: 9500 - 300 (eitc)     = 9200
  #   Step 3: 9200 - min(10, 10)    = 9190   (irregular=10, capped at 10)
  #   Step 4: 9190 - min(1000, 2410)= 8190   (student=1000, under monthly limit)
  #   Step 5: 8190 - min(20, 20)    = 8170
  #   Step 6: 8170 - min(8170, 65)  = 8105
  #   Step 7: 8105 - 200 (irwe)     = 7905
  #   Step 8: 7905 × 0.5            = 3952.50
  #   Step 9: 3952.50 - 100 (blind) = 3852.50
  #   Step 10: 3852.50 - 50 (ss)    = 3802.50
  # ---------------------------------------------------------------------------
  - case_id: "edg_002"
    description: "All 10 exclusion steps: federal ($500), EITC ($300), irregular ($10), student ($1,000), $20 general, $65 earned, IRWE ($200), one-half, blind work ($100), self-support ($50). Result: $3,802.50"
    inputs:
      gross_earned_income: 10000
      federal_exclusions: 500
      eitc_exclusion: 300
      irregular_earned_income: 10
      student_earned_income: 1000
      student_monthly_limit: 2410
      non_needs_based_unearned_remainder: 20
      impairment_work_expenses: 200
      blind_work_expenses: 100
      self_support_exclusion: 50
    expected:
      adjusted_earned_income: 3802.50
    tags: ["edge", "allow", "all_steps", "combined_exclusions"]

  # ---------------------------------------------------------------------------
  # edg_003 — Student income exactly at the monthly limit (no cap fires)
  # student_earned_income = student_monthly_limit = 2410 → exclusion = exactly $2,410
  # gross=5000, student=2410, limit=2410, remainder=20:
  #   Step 4: 5000 - min(2410, 2410) = 2590
  #   Step 5: 2590 - 20 = 2570
  #   Step 6: 2570 - 65 = 2505
  #   Step 8: 2505 × 0.5 = 1252.50
  # Note: student_cap_001 tests student=3000 (above limit); this tests exact-at-limit.
  # ---------------------------------------------------------------------------
  - case_id: "edg_003"
    description: "Student earned income exactly at monthly limit ($2,410 = $2,410 cap): full $2,410 excluded, cap does not reduce. Result: (5000 - 2410 - 20 - 65) × 0.5 = $1,252.50"
    inputs:
      gross_earned_income: 5000
      student_earned_income: 2410
      student_monthly_limit: 2410
      non_needs_based_unearned_remainder: 20
    expected:
      adjusted_earned_income: 1252.50
    tags: ["edge", "allow", "step_4", "student_at_limit"]

  # ---------------------------------------------------------------------------
  # edg_004 — IRWE exactly consumes all income remaining after step 6
  # gross=200, remainder=20, irwe=115:
  #   Step 5: 200 - 20 = 180
  #   Step 6: 180 - min(180, 65) = 180 - 65 = 115
  #   Step 7: 115 - 115 (irwe exactly = after_65) = 0
  #   Step 8: 0 × 0.5 = 0
  #   adjusted = 0
  # Tests IRWE that exhausts all remaining income without going negative.
  # ---------------------------------------------------------------------------
  - case_id: "edg_004"
    description: "IRWE ($115) exactly equals remaining income after step 6 ($115): result is $0. Tests full exhaustion by IRWE without negative overflow."
    inputs:
      gross_earned_income: 200
      non_needs_based_unearned_remainder: 20
      impairment_work_expenses: 115
    expected:
      adjusted_earned_income: 0
    tags: ["edge", "allow", "step_7", "irwe_exhausts_income"]
