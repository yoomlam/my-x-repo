# Auto-generated by /expand-tests. Do not edit directly.
# Re-run /expand-tests to regenerate.
#
# Boundary tests for all unique income_limit values in the expanded_refused_cash_income_limits table.
# Tests N-1/N/N+1 around each threshold (adjusted_earned_income vs income_limit).
#
# Gross income formula (simple chain — steps 5+6+8 with full $20 remainder):
#   adjusted = (gross - 20 - 65) × 0.5  →  gross = adjusted × 2 + 85
#
# One representative household type per limit group:
#   Single/NHR group (A1E, B1E, H1E, NHR): use A1E
#   Couple group (A2S, B2S, H2S, A2C, B2C, H2C): use A2S
#
# DOL set to match client income (same gross, same exclusions) for all cases.
# This produces "both at/below/above" scenarios cleanly.
#
# Already covered: A1E 2026 N=1845 (allow_boundary_001 in eligibility_tests.yaml)
# This file adds N-1 and N+1 for A1E 2026, plus N-1/N/N+1 for all other limit groups.
test_suite:
  spec: "eligibility.civil.yaml"
  description: "Income limit boundary expanded tests — generated 2026-03-27"
  version: "1.0"

# =============================================================================
# Key reference values:
#   A1E 2024: income_limit = 1751   gross-at-N = 1751×2+85 = 3587
#   A1E 2025: income_limit = 1795   gross-at-N = 1795×2+85 = 3675  (drv_002 covers N=1795)
#   A1E 2026: income_limit = 1845   gross-at-N = 1845×2+85 = 3775  (allow_boundary_001 covers N)
#   A2S 2024: income_limit = 2593   gross-at-N = 2593×2+85 = 5271
#   A2S 2025: income_limit = 2658   gross-at-N = 2658×2+85 = 5401
#   A2S 2026: income_limit = 2732   gross-at-N = 2732×2+85 = 5549
# =============================================================================

tests:

  # ===========================================================================
  # A1E 2024 — income_limit = $1,751
  # ===========================================================================

  # bnd_001 — adjusted=1750 (just below 1751); gross=3585
  # (3585 - 20 - 65) × 0.5 = 3500 × 0.5 = 1750 < 1751 → approve
  - case_id: "bnd_001"
    description: "A1E 2024: client and DOL adjusted $1,750 — just below limit $1,751 — approve"
    inputs:
      ClientData.gross_earned_income: 3585
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2024
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 10755
      DOLRecord.gross_earned_income: 3585
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a1e_2024", "below_limit"]

  # bnd_002 — adjusted=1751 (exactly at 1751); gross=3587
  # (3587 - 20 - 65) × 0.5 = 3502 × 0.5 = 1751 = 1751 → approve (≤)
  - case_id: "bnd_002"
    description: "A1E 2024: client and DOL adjusted exactly $1,751 = income limit — approve (≤ limit passes)"
    inputs:
      ClientData.gross_earned_income: 3587
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2024
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 10761
      DOLRecord.gross_earned_income: 3587
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a1e_2024", "at_limit"]

  # bnd_003 — adjusted=1752 (just above 1751); gross=3589
  # (3589 - 20 - 65) × 0.5 = 3504 × 0.5 = 1752 > 1751 → deny (both above)
  - case_id: "bnd_003"
    description: "A1E 2024: client and DOL adjusted $1,752 — just above limit $1,751 — deny (both above standard)"
    inputs:
      ClientData.gross_earned_income: 3589
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2024
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 10767
      DOLRecord.gross_earned_income: 3589
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["boundary", "deny", "a1e_2024", "above_limit"]

  # ===========================================================================
  # A1E 2025 — income_limit = $1,795
  # Note: drv_002 (eligibility_derived_from_extracted_tests.yaml) covers N=1795.
  # This file adds N-1 and N+1 for completeness.
  # ===========================================================================

  # bnd_004 — adjusted=1794 (just below 1795); gross=3673
  # (3673 - 20 - 65) × 0.5 = 3588 × 0.5 = 1794 < 1795 → approve
  - case_id: "bnd_004"
    description: "A1E 2025: client and DOL adjusted $1,794 — just below limit $1,795 — approve"
    inputs:
      ClientData.gross_earned_income: 3673
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11019
      DOLRecord.gross_earned_income: 3673
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a1e_2025", "below_limit"]

  # bnd_005 — adjusted=1796 (just above 1795); gross=3677
  # (3677 - 20 - 65) × 0.5 = 3592 × 0.5 = 1796 > 1795 → deny (both above)
  - case_id: "bnd_005"
    description: "A1E 2025: client and DOL adjusted $1,796 — just above limit $1,795 — deny (both above standard)"
    inputs:
      ClientData.gross_earned_income: 3677
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11031
      DOLRecord.gross_earned_income: 3677
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["boundary", "deny", "a1e_2025", "above_limit"]

  # ===========================================================================
  # A1E 2026 — income_limit = $1,845
  # N=1845 (gross=3775) is already in allow_boundary_001 in eligibility_tests.yaml.
  # Adding N-1 (gross=3773) and N+1 (gross=3777).
  # ===========================================================================

  # bnd_006 — adjusted=1844 (just below 1845); gross=3773
  # (3773 - 20 - 65) × 0.5 = 3688 × 0.5 = 1844 < 1845 → approve
  - case_id: "bnd_006"
    description: "A1E 2026: client and DOL adjusted $1,844 — just below limit $1,845 — approve"
    inputs:
      ClientData.gross_earned_income: 3773
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11319
      DOLRecord.gross_earned_income: 3773
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a1e_2026", "below_limit"]

  # bnd_007 — adjusted=1846 (just above 1845); gross=3777
  # (3777 - 20 - 65) × 0.5 = 3692 × 0.5 = 1846 > 1845 → deny (both above)
  - case_id: "bnd_007"
    description: "A1E 2026: client and DOL adjusted $1,846 — just above limit $1,845 — deny (both above standard)"
    inputs:
      ClientData.gross_earned_income: 3777
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11331
      DOLRecord.gross_earned_income: 3777
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["boundary", "deny", "a1e_2026", "above_limit"]

  # ===========================================================================
  # A2S 2024 — income_limit = $2,593
  # ===========================================================================

  # bnd_008 — adjusted=2592 (just below 2593); gross=5269
  # (5269 - 20 - 65) × 0.5 = 5184 × 0.5 = 2592 < 2593 → approve
  - case_id: "bnd_008"
    description: "A2S 2024: client and DOL adjusted $2,592 — just below couple limit $2,593 — approve"
    inputs:
      ClientData.gross_earned_income: 5269
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2024
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 15807
      DOLRecord.gross_earned_income: 5269
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a2s_2024", "couple_type", "below_limit"]

  # bnd_009 — adjusted=2593 (exactly at 2593); gross=5271
  # (5271 - 20 - 65) × 0.5 = 5186 × 0.5 = 2593 = 2593 → approve (≤)
  - case_id: "bnd_009"
    description: "A2S 2024: client and DOL adjusted exactly $2,593 = couple income limit — approve (≤ limit passes)"
    inputs:
      ClientData.gross_earned_income: 5271
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2024
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 15813
      DOLRecord.gross_earned_income: 5271
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a2s_2024", "couple_type", "at_limit"]

  # bnd_010 — adjusted=2594 (just above 2593); gross=5273
  # (5273 - 20 - 65) × 0.5 = 5188 × 0.5 = 2594 > 2593 → deny (both above)
  - case_id: "bnd_010"
    description: "A2S 2024: client and DOL adjusted $2,594 — just above couple limit $2,593 — deny"
    inputs:
      ClientData.gross_earned_income: 5273
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2024
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 15819
      DOLRecord.gross_earned_income: 5273
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["boundary", "deny", "a2s_2024", "couple_type", "above_limit"]

  # ===========================================================================
  # A2S 2025 — income_limit = $2,658
  # ===========================================================================

  # bnd_011 — adjusted=2657 (just below 2658); gross=5399
  # (5399 - 20 - 65) × 0.5 = 5314 × 0.5 = 2657 < 2658 → approve
  - case_id: "bnd_011"
    description: "A2S 2025: client and DOL adjusted $2,657 — just below couple limit $2,658 — approve"
    inputs:
      ClientData.gross_earned_income: 5399
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 16197
      DOLRecord.gross_earned_income: 5399
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a2s_2025", "couple_type", "below_limit"]

  # bnd_012 — adjusted=2658 (exactly at 2658); gross=5401
  # (5401 - 20 - 65) × 0.5 = 5316 × 0.5 = 2658 = 2658 → approve (≤)
  - case_id: "bnd_012"
    description: "A2S 2025: client and DOL adjusted exactly $2,658 = couple income limit — approve (≤ limit passes)"
    inputs:
      ClientData.gross_earned_income: 5401
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 16203
      DOLRecord.gross_earned_income: 5401
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a2s_2025", "couple_type", "at_limit"]

  # bnd_013 — adjusted=2659 (just above 2658); gross=5403
  # (5403 - 20 - 65) × 0.5 = 5318 × 0.5 = 2659 > 2658 → deny (both above)
  - case_id: "bnd_013"
    description: "A2S 2025: client and DOL adjusted $2,659 — just above couple limit $2,658 — deny"
    inputs:
      ClientData.gross_earned_income: 5403
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 16209
      DOLRecord.gross_earned_income: 5403
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["boundary", "deny", "a2s_2025", "couple_type", "above_limit"]

  # ===========================================================================
  # A2S 2026 — income_limit = $2,732
  # ===========================================================================

  # bnd_014 — adjusted=2731 (just below 2732); gross=5547
  # (5547 - 20 - 65) × 0.5 = 5462 × 0.5 = 2731 < 2732 → approve
  - case_id: "bnd_014"
    description: "A2S 2026: client and DOL adjusted $2,731 — just below couple limit $2,732 — approve"
    inputs:
      ClientData.gross_earned_income: 5547
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 16641
      DOLRecord.gross_earned_income: 5547
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a2s_2026", "couple_type", "below_limit"]

  # bnd_015 — adjusted=2732 (exactly at 2732); gross=5549
  # (5549 - 20 - 65) × 0.5 = 5464 × 0.5 = 2732 = 2732 → approve (≤)
  - case_id: "bnd_015"
    description: "A2S 2026: client and DOL adjusted exactly $2,732 = couple income limit — approve (≤ limit passes)"
    inputs:
      ClientData.gross_earned_income: 5549
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 16647
      DOLRecord.gross_earned_income: 5549
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["boundary", "allow", "a2s_2026", "couple_type", "at_limit"]

  # bnd_016 — adjusted=2733 (just above 2732); gross=5551
  # (5551 - 20 - 65) × 0.5 = 5466 × 0.5 = 2733 > 2732 → deny (both above)
  - case_id: "bnd_016"
    description: "A2S 2026: client and DOL adjusted $2,733 — just above couple limit $2,732 — deny"
    inputs:
      ClientData.gross_earned_income: 5551
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A2S"
      ClientData.benefit_year: 2026
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 16653
      DOLRecord.gross_earned_income: 5551
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "deny"
      reasons:
        - code: "INCOME_EXCEEDS_STANDARD_BOTH"
    tags: ["boundary", "deny", "a2s_2026", "couple_type", "above_limit"]
