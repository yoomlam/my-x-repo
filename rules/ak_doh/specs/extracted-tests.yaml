# Auto-generated by /create-tests. Do not edit directly —
# re-run /create-tests and choose "re-extract" to refresh.
extracted_tests:

  - case_id: "ext_001"
    description: "523B example: client $3,500 gross (adjusted $1,707.50) below A1E 2025 standard ($1,795); DOL quarterly $11,200 (adjusted $1,824.17) above standard but within 10% band — approve"
    source:
      file: "domains/ak_doh/input/policy_docs/adltc_manual/523 MEDICAID EXCEPTIONS TO APA INCOME POLICY.md"
      section: "523B — Reasonable Compatibility example"
    inputs:
      ClientData.gross_earned_income: 3500
      ClientData.non_needs_based_unearned_remainder: 20
      ClientData.household_type: "A1E"
      ClientData.benefit_year: 2025
      DOLRecord.dol_earned_income_available: true
      DOLRecord.dol_quarterly_earnings: 11200
      DOLRecord.gross_earned_income: 3733.33
      DOLRecord.non_needs_based_unearned_remainder: 20
    expected:
      eligible: "approve"
      reasons: []
    tags: ["extracted", "allow", "action_4", "compatible"]
    notes: |
      Document traces: client $3,500 → -$20 → -$65 → ×0.5 = $1,707.50.
      DOL monthly avg = $11,200/3 = $3,733.33 → -$20 → -$65 → ×0.5 = $1,824.17.
      10% of $1,707.50 = $170.75; range $1,536.75–$1,878.25; DOL $1,824.17 is within range → compatible.
      Document does not state the household type or income limit; A1E 2025 ($1,795) is inferred to place
      client below and DOL above the standard, matching the action-4 scenario described.
